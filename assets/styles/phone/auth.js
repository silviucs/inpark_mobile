import React from 'react'
import {StyleSheet} from 'react-native';
import {MAIN_COLOUR} from '../../../src/system/constants';

export const auth = StyleSheet.create({

    textbox: {
        borderColor: MAIN_COLOUR,
        borderWidth: 1,
        opacity: 0.9
    }
})
